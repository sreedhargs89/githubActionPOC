name: Check Ticket Number
on:
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch:
    inputs:
      manual-trigger:
        description: 'Manually trigger the workflow'
        default: 'false'
jobs:
  check-ticket:
    runs-on: ubuntu-latest

    steps:
      - name: Check PR Description
        id: check-description
        run: |
          pattern="HCPCM-[0-9]+"
          title_matched=$(grep -qE "$pattern" <<< "${{ github.event.pull_request.title }}"; echo $?)
          description_matched=$(grep -qE "$pattern" <<< "${{ github.event.pull_request.body }}"; echo $?)

          if [ "$title_matched" -ne 0 -a "$description_matched" -ne 0 ]; then
            echo "::set-output name=ticket-missing::true"
          fi
      - name: Add Comment if Ticket is Missing
        if: steps.check-description.outputs.ticket-missing == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          COMMENT_BODY="Please include a valid HCPCM ticket number in the PR description."
          PR_NUMBER=${{ github.event.pull_request.number }}
          curl -X POST -H "Authorization: Bearer $GITHUB_TOKEN" \
               -H "Content-Type: application/json" \
               -d "{\"body\": \"$COMMENT_BODY\"}" \
               "https://api.github.com/repos/${{ github.repository }}/issues/$PR_NUMBER/comments"
